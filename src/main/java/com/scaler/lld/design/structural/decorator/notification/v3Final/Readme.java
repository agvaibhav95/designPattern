package com.scaler.lld.design.structural.decorator.notification.v3Final;

public class Readme {
    // so inheritance is a very static approach means
    //suppose class A extends class B can i do like class C also extends B
    //can i change this at runtime
    //is inheritance dynamic?? --> NO, we will use composition
    // we will use a composition



    // so we want to send email always this is our use case

    //so we can create email as a base class and on top of that we can decorate with sms, slack and push

    // we will be using email as base class as this is our use case requirement i.e we need to send email everytime


}
